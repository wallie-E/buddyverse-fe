# 微信交换功能完整流程

## 功能概述
微信交换功能采用双向确认机制，确保双方都同意后才能看到对方的微信号。

---

## 完整流程

### 📝 第一步：配置微信号（必需）

**页面位置**：`/profile` (个人信息页)

**操作步骤**：
1. 点击"编辑资料"按钮
2. 在"微信号"输入框中填写微信号
3. 点击"保存"按钮

**调用的API**：
- `PUT /api/users/profile` - 更新用户信息（包含 wechat_id 字段）

**说明**：
- 微信号字段名为 `wechat_id`
- 只有配置了微信号才能发起或确认交换请求

---

### 🚀 第二步：发起交换请求

**页面位置**：`/post/:id` (帖子详情页)

**触发位置**：
1. **帖子互动区域**：点击帖子作者旁边的"交换微信"按钮
   - 条件：当前用户不是帖子作者
   
2. **评论区域**：点击评论者旁边的"交换微信"按钮
   - 条件：当前用户不是评论作者

**操作流程**：

#### 场景A：首次发起交换
1. 点击"交换微信"按钮
2. 弹窗打开，自动调用 `GET /api/wechat-exchange/info?targetUserId={目标用户ID}`
3. 如果返回 `exists: false`，显示"确定与对方交换微信吗？"
4. 点击"确定"按钮
5. 调用 `POST /api/wechat-exchange/request`
   ```json
   {
     "targetUserId": 123,
     "wechatId": "my_wechat_123",
     "isUpdate": false
   }
   ```
6. 成功后显示"交换微信请求已发送"
7. 系统自动创建通知发送给接收人

#### 场景B：等待对方确认（重新发送）
1. 点击"交换微信"按钮
2. 弹窗打开，调用 `GET /api/wechat-exchange/info`
3. 如果返回 `status: 0, waitingFor: 'other'`，显示"等待对方确认"
4. 点击"重新发送"按钮
5. 调用 `POST /api/wechat-exchange/request`（`isUpdate: true`）
6. 成功后显示"交换请求已重新发送"
7. 更新交换时间，重新发送通知

#### 场景C：已经交换成功
1. 点击"交换微信"按钮
2. 弹窗打开，调用 `GET /api/wechat-exchange/info`
3. 如果返回 `status: 1`，显示"交换成功"
4. 展示对方的微信号
5. 点击"确定"关闭弹窗

**API调用**：
- `GET /api/wechat-exchange/info?targetUserId={目标用户ID}` - 获取交换信息
- `POST /api/wechat-exchange/request` - 发起/更新交换请求

---

### ✅ 第三步：确认交换（接收方）

**页面位置**：`/notifications` (通知中心)

**操作流程**：

#### 方式1：通过通知中心
1. 进入通知中心页面
2. 切换到"微信交换"标签页
3. 查看交换记录列表（调用 `GET /api/wechat-exchange/my-exchanges`）
4. 点击未交换成功的记录（`status !== 1`）
5. 弹窗打开，自动调用 `GET /api/wechat-exchange/info?targetUserId={对方用户ID}`
6. 如果 `status: 0, waitingFor: 'me'`，显示"确认交换微信"
7. 点击"确认交换"按钮
8. 调用 `POST /api/wechat-exchange/confirm`
   ```json
   {
     "exchangeNo": "WX17001234567890001",
     "wechatId": "my_wechat_456",
     "action": "accept"
   }
   ```
9. 成功后显示"交换成功！"
10. 弹窗更新，显示对方的微信号
11. 列表中的记录状态更新为"已完成"

#### 方式2：通过通知消息（如果实现了通知点击跳转）
1. 收到微信交换请求通知
2. 点击通知跳转到通知中心
3. 后续流程同方式1

**API调用**：
- `GET /api/wechat-exchange/my-exchanges` - 获取我的交换记录列表
- `GET /api/wechat-exchange/info?targetUserId={对方用户ID}` - 获取交换详情
- `POST /api/wechat-exchange/confirm` - 确认交换

---

### 📋 第四步：查看交换记录

**页面位置**：`/notifications` (通知中心) → "微信交换"标签页

**显示内容**：
- 交换记录列表（分页加载）
- 每条记录显示：
  - 对方昵称
  - 交换状态（待确认/已完成/已拒绝/已过期）
  - 创建时间
  - 完成时间（如果已完成）
  - **对方的微信号**（仅当 status === 1 时显示）

**交互规则**：
- **已交换成功**（status === 1）：不弹窗，微信号直接在列表中显示
- **未交换成功**（status !== 1）：点击后弹出弹窗，可查看详情或确认交换

**API调用**：
- `GET /api/wechat-exchange/my-exchanges?page=1&limit=20` - 获取交换记录列表

---

## 状态流转图

```
未交换 (exists: false)
    ↓ [发起请求]
待确认 (status: 0, waitingFor: 'other') ← 发起人视角
    ↓ [对方确认]
已完成 (status: 1) ✅
    ↓
双方可看到对方微信号

待确认 (status: 0, waitingFor: 'me') ← 接收人视角
    ↓ [确认交换]
已完成 (status: 1) ✅
    ↓
双方可看到对方微信号
```

---

## 关键API接口总结

| 接口 | 方法 | 用途 | 调用位置 |
|------|------|------|----------|
| `/api/users/profile` | PUT | 更新用户信息（配置微信号） | ProfilePage |
| `/api/wechat-exchange/info` | GET | 获取交换信息 | PostDetailPage, NotificationsPage |
| `/api/wechat-exchange/request` | POST | 发起/更新交换请求 | PostDetailPage |
| `/api/wechat-exchange/confirm` | POST | 确认交换（接受/拒绝） | NotificationsPage |
| `/api/wechat-exchange/my-exchanges` | GET | 获取我的交换记录列表 | NotificationsPage |

---

## 功能完整性检查清单

✅ **已实现的功能**：
- [x] 个人信息页配置微信号
- [x] 帖子详情页发起交换请求
- [x] 帖子详情页重新发送请求
- [x] 帖子详情页查看已完成的交换
- [x] 通知中心查看交换记录列表
- [x] 通知中心确认交换
- [x] 通知中心查看已完成的交换详情
- [x] 微信号检查（未配置时提示跳转）
- [x] 状态管理（待确认、已完成、已拒绝、已过期）
- [x] 通知系统集成（后端自动发送通知）

✅ **UI/UX**：
- [x] 加载状态显示
- [x] 错误处理和提示
- [x] 按钮禁用状态
- [x] 空状态提示
- [x] 分页加载

---

## 注意事项

1. **微信号配置**：用户必须先配置微信号才能发起或确认交换
2. **权限控制**：
   - 不能与自己交换微信
   - 帖子作者看不到自己的"交换微信"按钮
   - 评论作者看不到自己的"交换微信"按钮
3. **状态判断**：
   - 已交换成功（status === 1）的记录不弹窗
   - 只有未交换成功的记录才可点击查看详情
4. **数据更新**：
   - 确认交换后自动更新列表和弹窗状态
   - 重新发送请求后更新交换时间

---

## 测试建议

### 测试场景1：完整交换流程
1. 用户A配置微信号
2. 用户A在帖子详情页向用户B发起交换
3. 用户B在通知中心看到交换记录
4. 用户B点击记录确认交换
5. 双方都能看到对方的微信号

### 测试场景2：重新发送请求
1. 用户A发起交换请求
2. 用户A再次点击"交换微信"
3. 点击"重新发送"
4. 验证交换时间更新

### 测试场景3：已完成的交换
1. 完成一次交换
2. 在帖子详情页点击"交换微信"
3. 验证直接显示对方微信号，不弹窗
4. 在通知中心验证已完成的记录不弹窗

### 测试场景4：未配置微信号
1. 未配置微信号的用户尝试发起交换
2. 验证提示跳转到个人信息页
3. 配置微信号后重新尝试

---

**功能状态**：✅ **完整实现**

所有核心功能已实现，包括发起交换、确认交换、查看记录等完整流程。

