# Copy to .env / .env.production locally. Do not commit real values.

# Dev server proxy target (backend origin)
VITE_API_PROXY_TARGET=http://localhost:8080

# Vite dev server port
VITE_API_PORT=3000

# Used by the client as API origin when set
VITE_API_URL=http://localhost:8080
