# 社交帖子平台完整接口文档

## 基础信息

- **Base URL**: `http://localhost:3000/api`
- **认证方式**: Bearer Token (JWT)
- **数据格式**: JSON
- **编码**: UTF-8

## 通用响应格式

### 成功响应
```json
{
  "success": true,
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### 错误响应
```json
{
  "success": false,
  "code": 400,
  "message": "错误信息",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

---

## 1. 认证相关接口

### 1.1 用户注册

- **请求路径**: `/api/auth/register`
- **请求方式**: `POST`
- **接口描述**: 用户通过邮箱和密码注册新账户
- **是否需要认证**: 否

**请求参数**:
```json
{
  "email": "string",      // 必填，邮箱地址，唯一
  "password": "string",   // 必填，密码，6-20位
  "nickname": "string"    // 必填，昵称，2-20位
}
```

**响应数据**:
```json
{
  "success": true,
  "code": 201,
  "message": "注册成功",
  "data": {
    "user": {
      "id": 1,
      "email": "user@example.com",
      "nickname": "用户昵称",
      "gender": "other",
      "avatar": null,
      "signature": null,
      "role": "user"
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `400` - 参数验证失败
- `409` - 邮箱已被注册

---

### 1.2 用户登录

- **请求路径**: `/api/auth/login`
- **请求方式**: `POST`
- **接口描述**: 用户通过邮箱和密码登录系统
- **是否需要认证**: 否

**请求参数**:
```json
{
  "email": "string",      // 必填，邮箱地址
  "password": "string"    // 必填，密码
}
```

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "登录成功",
  "data": {
    "user": {
      "id": 1,
      "email": "user@example.com",
      "nickname": "用户昵称",
      "gender": "male",
      "avatar": "头像URL",
      "signature": "个人签名",
      "role": "user"
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `400` - 参数验证失败
- `401` - 邮箱或密码错误
- `401` - 账户已被禁用

---

### 1.3 获取当前用户信息

- **请求路径**: `/api/auth/profile`
- **请求方式**: `GET`
- **接口描述**: 获取当前登录用户的详细信息
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取用户信息成功",
  "data": {
    "id": 1,
    "email": "user@example.com",
    "nickname": "用户昵称",
    "gender": "male",
    "avatar": "头像URL",
    "signature": "个人签名",
    "role": "user",
    "created_at": "2024-01-01T00:00:00.000Z"
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `404` - 用户不存在

---

## 2. 用户管理接口

### 2.1 获取用户资料

- **请求路径**: `/api/users/profile`
- **请求方式**: `POST`
- **接口描述**: 获取指定用户的公开资料信息和发布的帖子列表
- **是否需要认证**: 否

**请求参数**:
```json
{
  "user_id": "number",  // 必填，用户ID
  "page": "number",     // 可选，帖子页码，默认1
  "limit": "number"     // 可选，每页帖子数量，默认10
}
```

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取用户资料成功",
  "data": {
    "id": 1,
    "nickname": "用户昵称",
    "gender": "male",
    "avatar": "头像URL",
    "signature": "个人签名",
    "created_at": "2024-01-01T00:00:00.000Z",
    "posts": {
      "list": [
        {
          "id": 1,
          "content": "帖子内容",
          "location": "发布位置",
          "comment_count": 5,
          "comment_visibility": "public",
          "created_at": "2024-01-01T00:00:00.000Z",
          "category_name": "干饭搭子",
          "subcategory_name": "火锅"
        }
      ],
      "pagination": {
        "total": 20,
        "page": 1,
        "limit": 10,
        "pages": 2
      }
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `400` - 用户ID无效
- `404` - 用户不存在

---

### 2.2 更新用户信息

- **请求路径**: `/api/users/profile`
- **请求方式**: `PUT`
- **接口描述**: 更新当前用户的个人信息
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**请求参数**:
```json
{
  "nickname": "string",   // 可选，昵称，2-20位
  "gender": "string",     // 可选，性别：male/female/other
  "signature": "string"   // 可选，个人签名，最大200字符
}
```

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 1,
    "email": "user@example.com",
    "nickname": "新昵称",
    "gender": "male",
    "avatar": "头像URL",
    "signature": "新签名",
    "role": "user"
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `400` - 参数验证失败或没有要更新的字段
- `401` - 未认证或token无效

---

### 2.2 获取用户发布的帖子

- **请求路径**: `/api/users/posts`
- **请求方式**: `GET`
- **接口描述**: 获取当前用户发布的帖子列表
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**查询参数**:
- `page` (可选): 页码，默认1
- `limit` (可选): 每页数量，默认10

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "content": "帖子内容",
        "location": "发布位置",
        "comment_count": 5,
        "created_at": "2024-01-01T00:00:00.000Z",
        "category_name": "干饭搭子",
        "subcategory_name": "火锅"
      }
    ],
    "pagination": {
      "total": 20,
      "page": 1,
      "limit": 10,
      "pages": 2
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效

---

## 3. 帖子管理接口

### 3.1 创建帖子

- **请求路径**: `/api/posts`
- **请求方式**: `POST`
- **接口描述**: 发布新帖子
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**请求参数**:
```json
{
  "content": "string",           // 必填，帖子内容，1-150字
  "location": "string",          // 可选，发布位置，最大200字符
  "category_id": "number",       // 必填，主分类ID
  "subcategory_id": "number",    // 必填，细分类型ID
  "comment_visibility": "string" // 可选，评论可见性：public/private，默认public
}
```

**响应数据**:
```json
{
  "success": true,
  "code": 201,
  "message": "发布成功",
  "data": {
    "id": 1,
    "content": "帖子内容",
    "location": "发布位置",
    "comment_visibility": "public",
    "comment_count": 0,
    "created_at": "2024-01-01T00:00:00.000Z",
    "author_name": "用户昵称",
    "category_name": "干饭搭子",
    "subcategory_name": "火锅"
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `400` - 参数验证失败或分类不存在
- `401` - 未认证或token无效

---

### 3.2 获取帖子列表

- **请求路径**: `/api/posts`
- **请求方式**: `GET`
- **接口描述**: 获取帖子列表，支持分页和分类筛选
- **是否需要认证**: 否（可选认证）

**查询参数**:
- `page` (可选): 页码，默认1
- `limit` (可选): 每页数量，默认10
- `category_id` (可选): 主分类ID筛选
- `subcategory_id` (可选): 细分类型ID筛选

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "user_id": 1,
        "content": "帖子内容",
        "location": "发布位置",
        "comment_count": 5,
        "created_at": "2024-01-01T00:00:00.000Z",
        "author_name": "用户昵称",
        "category_name": "干饭搭子",
        "subcategory_name": "火锅"
      }
    ],
    "pagination": {
      "total": 100,
      "page": 1,
      "limit": 10,
      "pages": 10
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `500` - 服务器内部错误

---

### 3.3 获取帖子详情

- **请求路径**: `/api/posts/:id`
- **请求方式**: `GET`
- **接口描述**: 获取指定帖子的详细信息
- **是否需要认证**: 否（可选认证）

**路径参数**:
- `id`: 帖子ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "content": "帖子内容",
    "location": "发布位置",
    "comment_visibility": "public",
    "comment_count": 5,
    "created_at": "2024-01-01T00:00:00.000Z",
    "user_id": 2,
    "author_name": "用户昵称",
    "category_name": "干饭搭子",
    "subcategory_name": "火锅"
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `404` - 帖子不存在

---

### 3.4 删除帖子

- **请求路径**: `/api/posts/:id`
- **请求方式**: `DELETE`
- **接口描述**: 删除指定帖子（仅作者或管理员可删除）
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**路径参数**:
- `id`: 帖子ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 没有权限删除此帖子
- `404` - 帖子不存在

---

## 4. 评论管理接口

### 4.1 创建评论

- **请求路径**: `/api/comments`
- **请求方式**: `POST`
- **接口描述**: 对帖子进行评论或回复评论
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**请求参数**:
```json
{
  "post_id": "number",    // 必填，帖子ID
  "content": "string",    // 必填，评论内容，1-500字
}
```

**响应数据**:
```json
{
  "success": true,
  "code": 201,
  "message": "评论成功",
  "data": {
    "id": 1,
    "content": "评论内容",
    "created_at": "2024-01-01T00:00:00.000Z",
    "author_name": "用户昵称"
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `400` - 参数验证失败或父评论不存在
- `401` - 未认证或token无效
- `403` - 该帖子评论仅作者可见
- `404` - 帖子不存在

---

### 4.2 获取帖子评论列表

- **请求路径**: `/api/comments/post/:postId`
- **请求方式**: `GET`
- **接口描述**: 获取指定帖子的评论列表（包含一级评论和二级回复）
- **是否需要认证**: 否（可选认证）

**路径参数**:
- `postId`: 帖子ID

**查询参数**:
- `page` (可选): 页码，默认1
- `limit` (可选): 每页数量，默认10

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "user_id": 1,
        "content": "一级评论内容",
        "created_at": "2024-01-01T00:00:00.000Z",
        "author_name": "用户昵称",
        "replies": [
          {
            "id": 2,
            "user_id": 2,
            "content": "二级回复内容",
            "created_at": "2024-01-01T00:01:00.000Z",
            "author_name": "回复者昵称"
          }
        ]
      }
    ],
    "pagination": {
      "total": 20,
      "page": 1,
      "limit": 10,
      "pages": 2
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `403` - 该帖子评论仅作者可见
- `404` - 帖子不存在

---

### 4.3 删除评论

- **请求路径**: `/api/comments/:id`
- **请求方式**: `DELETE`
- **接口描述**: 删除指定评论（仅作者或管理员可删除）
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**路径参数**:
- `id`: 评论ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 没有权限删除此评论
- `404` - 评论不存在

---

## 5. 分类管理接口

### 5.1 获取所有分类

- **请求路径**: `/api/categories`
- **请求方式**: `GET`
- **接口描述**: 获取所有帖子分类及其细分类型
- **是否需要认证**: 否

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "name": "干饭搭子",
      "description": "寻找一起吃饭的伙伴",
      "sort_order": 1,
      "subcategories": [
        {
          "id": 1,
          "name": "火锅",
          "description": null,
          "sort_order": 1
        },
        {
          "id": 2,
          "name": "烧烤",
          "description": null,
          "sort_order": 2
        }
      ]
    }
  ],
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `500` - 服务器内部错误

---

### 5.2 获取指定分类的细分类型

- **请求路径**: `/api/categories/:categoryId/subcategories`
- **请求方式**: `GET`
- **接口描述**: 获取指定主分类下的所有细分类型
- **是否需要认证**: 否

**路径参数**:
- `categoryId`: 主分类ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "category": {
      "id": 1,
      "name": "干饭搭子"
    },
    "subcategories": [
      {
        "id": 1,
        "name": "火锅",
        "description": null,
        "sort_order": 1
      },
      {
        "id": 2,
        "name": "烧烤",
        "description": null,
        "sort_order": 2
      }
    ]
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `404` - 分类不存在

---

## 6. 通知管理接口

### 6.1 获取通知列表

- **请求路径**: `/api/notifications`
- **请求方式**: `GET`
- **接口描述**: 获取当前用户的通知列表
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**查询参数**:
- `page` (可选): 页码，默认1
- `limit` (可选): 每页数量，默认20
- `type` (可选): 通知类型筛选：comment/reply/system

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "type": "comment",
        "title": "帖子评论",
        "content": "有人评论了您的帖子：很有趣的内容",
        "related_id": 5,
        "related_type": "comment",
        "is_read": false,
        "created_at": "2024-01-01T00:00:00.000Z"
      }
    ],
    "pagination": {
      "total": 50,
      "page": 1,
      "limit": 20,
      "pages": 3
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效

---

### 6.2 获取未读通知数量

- **请求路径**: `/api/notifications/unread-count`
- **请求方式**: `GET`
- **接口描述**: 获取当前用户的未读通知数量
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "count": 5
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效

---

### 6.3 标记通知为已读

- **请求路径**: `/api/notifications/:id/read`
- **请求方式**: `PUT`
- **接口描述**: 将指定通知标记为已读
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**路径参数**:
- `id`: 通知ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "标记成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `404` - 通知不存在

---

### 6.4 标记所有通知为已读

- **请求路径**: `/api/notifications/read-all`
- **请求方式**: `PUT`
- **接口描述**: 将当前用户的所有未读通知标记为已读
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "全部标记成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效

---

### 6.5 删除通知

- **请求路径**: `/api/notifications/:id`
- **请求方式**: `DELETE`
- **接口描述**: 删除指定通知
- **是否需要认证**: 是

**请求头**:
```
Authorization: Bearer <token>
```

**路径参数**:
- `id`: 通知ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `404` - 通知不存在

---

## 7. 管理员接口

> 🔒 以下所有接口都需要管理员权限

### 7.1 获取统计数据

- **请求路径**: `/api/admin/stats`
- **请求方式**: `GET`
- **接口描述**: 获取平台统计数据（用户、帖子、评论等）
- **是否需要认证**: 是（需要管理员权限）

**请求头**:
```
Authorization: Bearer <token>
```

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取统计数据成功",
  "data": {
    "users": {
      "total_users": 1000,
      "active_users": 950,
      "admin_users": 5
    },
    "posts": {
      "total_posts": 5000,
      "active_posts": 4800,
      "today_posts": 50
    },
    "comments": {
      "total_comments": 15000,
      "active_comments": 14500,
      "today_comments": 120
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 需要管理员权限

---

### 7.2 获取用户列表

- **请求路径**: `/api/admin/users`
- **请求方式**: `GET`
- **接口描述**: 获取平台所有用户列表
- **是否需要认证**: 是（需要管理员权限）

**请求头**:
```
Authorization: Bearer <token>
```

**查询参数**:
- `page` (可选): 页码，默认1
- `limit` (可选): 每页数量，默认20
- `status` (可选): 用户状态筛选：active/inactive/banned
- `role` (可选): 用户角色筛选：user/admin
- `search` (可选): 搜索关键词，支持按用户昵称或邮箱搜索

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "email": "user@example.com",
        "nickname": "用户昵称",
        "gender": "male",
        "role": "user",
        "status": "active",
        "created_at": "2024-01-01T00:00:00.000Z",
        "post_count": 5
      }
    ],
    "pagination": {
      "total": 1000,
      "page": 1,
      "limit": 20,
      "pages": 50
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 需要管理员权限

**使用示例**:
```bash
# 获取所有用户（分页）
GET /api/admin/users?page=1&limit=20

# 按状态筛选用户
GET /api/admin/users?status=active

# 按角色筛选用户
GET /api/admin/users?role=user

# 按昵称搜索用户
GET /api/admin/users?search=测试

# 按邮箱搜索用户
GET /api/admin/users?search=qq.com

# 组合搜索：搜索 + 状态筛选
GET /api/admin/users?search=测试&status=active

# 组合搜索：搜索 + 角色筛选
GET /api/admin/users?search=example.com&role=user

# 完整搜索：搜索 + 状态 + 角色 + 分页
GET /api/admin/users?search=测试&status=active&role=user&page=1&limit=10
```

**字段说明**:
- `post_count`: 用户发布的活跃帖子数量（不包含已删除的帖子）

---

### 7.3 删除用户

- **请求路径**: `/api/admin/users/:id`
- **请求方式**: `DELETE`
- **接口描述**: 删除指定用户（不能删除管理员）
- **是否需要认证**: 是（需要管理员权限）

**请求头**:
```
Authorization: Bearer <token>
```

**路径参数**:
- `id`: 用户ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 需要管理员权限或不能删除管理员账户
- `404` - 用户不存在

---

### 7.4 修改用户状态

- **请求路径**: `/api/admin/users/:id/status`
- **请求方式**: `PUT`
- **接口描述**: 修改指定用户的状态（不能修改管理员状态）
- **是否需要认证**: 是（需要管理员权限）

**请求头**:
```
Authorization: Bearer <token>
```

**路径参数**:
- `id`: 用户ID

**请求参数**:
```json
{
  "status": "string"  // 必填，用户状态：active/inactive/banned
}
```

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "状态更新成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `400` - 状态值无效
- `401` - 未认证或token无效
- `403` - 需要管理员权限或不能修改管理员状态
- `404` - 用户不存在

---

### 7.5 获取帖子列表

- **请求路径**: `/api/admin/posts`
- **请求方式**: `GET`
- **接口描述**: 获取平台所有帖子列表，支持按用户ID筛选
- **是否需要认证**: 是（需要管理员权限）

**请求头**:
```
Authorization: Bearer <token>
```

**查询参数**:
- `page` (可选): 页码，默认1
- `limit` (可选): 每页数量，默认20
- `status` (可选): 帖子状态筛选：active/deleted
- `category_id` (可选): 分类ID筛选
- `user_id` (可选): 用户ID筛选，指定后返回该用户的基本信息和帖子信息

**响应数据**:

**不指定user_id时（获取所有帖子）**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "content": "帖子内容",
        "location": "发布位置",
        "comment_count": 5,
        "status": "active",
        "created_at": "2024-01-01T00:00:00.000Z",
        "author_name": "用户昵称",
        "author_email": "user@example.com",
        "category_name": "干饭搭子",
        "subcategory_name": "火锅"
      }
    ],
    "pagination": {
      "total": 5000,
      "page": 1,
      "limit": 20,
      "pages": 250
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**指定user_id时（获取指定用户的帖子）**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "user": {
      "id": 1,
      "email": "user@example.com",
      "nickname": "用户昵称",
      "gender": "male",
      "role": "user",
      "status": "active",
      "created_at": "2024-01-01T00:00:00.000Z"
    },
    "posts": {
      "list": [
        {
          "id": 1,
          "content": "帖子内容",
          "location": "发布位置",
          "comment_count": 5,
          "status": "active",
          "created_at": "2024-01-01T00:00:00.000Z",
          "author_name": "用户昵称",
          "author_email": "user@example.com",
          "category_name": "干饭搭子",
          "subcategory_name": "火锅"
        }
      ],
      "pagination": {
        "total": 10,
        "page": 1,
        "limit": 20,
        "pages": 1
      }
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 需要管理员权限
- `404` - 用户不存在（当指定user_id但用户不存在时）

**使用示例**:
```bash
# 获取所有帖子
GET /api/admin/posts?page=1&limit=20

# 按状态筛选帖子
GET /api/admin/posts?status=active

# 按分类筛选帖子
GET /api/admin/posts?category_id=1

# 获取指定用户的帖子（返回用户信息和帖子信息）
GET /api/admin/posts?user_id=1

# 组合筛选：指定用户 + 状态筛选
GET /api/admin/posts?user_id=1&status=active

# 组合筛选：指定用户 + 分类筛选
GET /api/admin/posts?user_id=1&category_id=1

# 完整筛选：指定用户 + 状态 + 分页
GET /api/admin/posts?user_id=1&status=active&page=1&limit=10
```

---

### 7.6 删除帖子

- **请求路径**: `/api/admin/posts/:id`
- **请求方式**: `DELETE`
- **接口描述**: 删除指定帖子
- **是否需要认证**: 是（需要管理员权限）

**请求头**:
```
Authorization: Bearer <token>
```

**路径参数**:
- `id`: 帖子ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 需要管理员权限
- `404` - 帖子不存在

---

### 7.7 获取评论列表

- **请求路径**: `/api/admin/comments`
- **请求方式**: `GET`
- **接口描述**: 获取平台所有评论列表
- **是否需要认证**: 是（需要管理员权限）

**请求头**:
```
Authorization: Bearer <token>
```

**查询参数**:
- `page` (可选): 页码，默认1
- `limit` (可选): 每页数量，默认20
- `status` (可选): 评论状态筛选：active/deleted
- `post_id` (可选): 帖子ID筛选

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "content": "评论内容",
        "status": "active",
        "created_at": "2024-01-01T00:00:00.000Z",
        "author_name": "用户昵称",
        "author_email": "user@example.com",
        "post_id": 1,
        "post_content": "帖子内容"
      }
    ],
    "pagination": {
      "total": 15000,
      "page": 1,
      "limit": 20,
      "pages": 750
    }
  },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 需要管理员权限

---

### 7.8 删除评论

- **请求路径**: `/api/admin/comments/:id`
- **请求方式**: `DELETE`
- **接口描述**: 删除指定评论
- **是否需要认证**: 是（需要管理员权限）

**请求头**:
```
Authorization: Bearer <token>
```

**路径参数**:
- `id`: 评论ID

**响应数据**:
```json
{
  "success": true,
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

**错误响应**:
- `401` - 未认证或token无效
- `403` - 需要管理员权限
- `404` - 评论不存在

---

## 8. 状态码说明

| 状态码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 201 | 创建成功 |
| 400 | 请求参数错误 |
| 401 | 未认证或认证失败 |
| 403 | 权限不足 |
| 404 | 资源不存在 |
| 409 | 资源冲突 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |

## 9. 错误码说明

常见错误信息：
- `邮箱已被注册`
- `邮箱或密码错误`
- `请先登录`
- `需要管理员权限`
- `帖子不存在`
- `评论不存在`
- `没有权限删除此帖子`
- `没有权限删除此评论`
- `分类不存在`
- `细分类型不存在或不属于该分类`
- `该帖子评论仅作者可见`
- `父评论不存在`
- `通知不存在`
- `用户不存在`
- `不能删除管理员账户`
- `不能修改管理员状态`

## 10. 数据字典

### 用户性别枚举
- `male` - 男性
- `female` - 女性
- `other` - 其他

### 用户角色枚举
- `user` - 普通用户
- `admin` - 管理员

### 用户状态枚举
- `active` - 正常
- `inactive` - 未激活
- `banned` - 已封禁

### 评论可见性枚举
- `public` - 公开
- `private` - 仅自己可见

### 通知类型枚举
- `comment` - 评论通知
- `reply` - 回复通知
- `system` - 系统通知

### 帖子/评论状态枚举
- `active` - 正常
- `deleted` - 已删除

## 11. 默认账户信息

**管理员账户**:
- 邮箱: `admin@example.com`
- 密码: `password`
- 角色: `admin`

**注意**: 生产环境请及时修改默认密码！

## 12. 安全说明

1. **认证机制**: 使用JWT Token进行认证
2. **密码安全**: 使用bcryptjs进行密码哈希
3. **API限流**: 15分钟内最多100次请求
4. **输入验证**: 使用Joi进行请求参数验证
5. **SQL注入防护**: 使用参数化查询
6. **跨域配置**: 支持CORS跨域请求
7. **安全头**: 使用helmet添加安全响应头 